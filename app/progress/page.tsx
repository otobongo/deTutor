export default function ProgressPage() {
  return (
    <main className="mx-auto flex min-h-screen max-w-2xl flex-col gap-4 p-8">
      <h1 className="text-3xl font-semibold">Progress</h1>
      <p data-testid="progress-placeholder">
        Session reports, weekly summaries, and the level dashboard arrive with Phase 3 (GT-308 to
        GT-310). Your session data is already being recorded.
      </p>
    </main>
  );
}
